source 'https://rubygems.org'

ruby '2.0.0'

gem 'foreman'
gem 'haml'
gem 'heroku-api'
gem 'rake'
gem 'sinatra'

group :presentation do
  # The version published to RubyGems has issues with GLI on OSX. Living on the
  # edge works for now.
  gem 'showoff', git: 'http://github.com/schacon/showoff.git'
end
